---
phase: 6-onechain-integration
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/wallet/utils.ts
  - lib/wallet/store.ts
  - hooks/useWallet.ts
  - components/layout/Header.tsx
  - __tests__/wallet/oneWallet.test.ts
autonomous: true
requirements:
  - WALLET-01
  - WALLET-02
  - WALLET-03
user_setup:
  - service: OneWallet
    why: "Wallet connection for OneChain transactions"
    env_vars: []
    dashboard_config:
      - task: "Install OneWallet browser extension"
        location: "https://wallet.onechain.gg/"

must_haves:
  truths:
    - "User can click Connect and OneWallet extension opens"
    - "After connecting, wallet address displays in header"
    - "OCT balance displays next to address"
    - "User can click Disconnect and wallet state clears"
  artifacts:
    - path: "lib/wallet/utils.ts"
      provides: "OneWallet connection via Wallet Standard API"
      exports: ["connectOneWallet", "findOneWallet", "fetchOctBalance"]
    - path: "lib/wallet/store.ts"
      provides: "Wallet state management"
      contains: "balance state"
    - path: "hooks/useWallet.ts"
      provides: "React hook for wallet operations"
      exports: ["useWallet"]
  key_links:
    - from: "hooks/useWallet.ts"
      to: "lib/wallet/utils.ts"
      via: "import and call connectOneWallet"
      pattern: "connectOneWallet\\(\\)"
    - from: "components/layout/Header.tsx"
      to: "hooks/useWallet.ts"
      via: "useWallet hook call"
      pattern: "useWallet\\(\\)"
---

<objective>
Replace mock wallet with real OneWallet integration using Wallet Standard API, implement balance fetching, and enable disconnect functionality.

Purpose: Enable users to connect their OneWallet extension, view their address and OCT balance, and disconnect when done. This is the foundation for all betting functionality.

Output: Working wallet connection with balance display, test coverage for wallet operations.
</objective>

<execution_context>
@C:/Users/Lenovo/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Lenovo/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/6-onechain-integration/06-RESEARCH.md
@.planning/phases/6-onechain-integration/06-VALIDATION.md
@lib/wallet/utils.ts
@lib/wallet/store.ts
@hooks/useWallet.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix Wallet Standard API connection</name>
  <files>lib/wallet/utils.ts</files>
  <action>
    Update the Wallet Standard API usage to use the correct feature namespace:

    1. Change `wallet.features['wallet:connect']` to `wallet.features['standard:connect']` per Wallet Standard specification
    2. Update the connect function signature to match the standard:
       - Input: `{ name: string, version: string, url: string }` (icon optional)
       - Output: `{ accounts: WalletAccount[] }`
    3. Add `standard:disconnect` feature support for proper disconnect
    4. Add `standard:signTransaction` feature stub for future transaction signing
    5. Keep `findOneWallet()` logic - it correctly identifies OneWallet by name

    The current code uses non-standard `wallet:connect` namespace. The Wallet Standard
    specification uses `standard:*` namespace for all features.
  </action>
  <verify>
    <automated>npm run build</automated>
  </verify>
  <done>
    - connectOneWallet() uses `standard:connect` feature
    - Disconnect feature stub added
    - TypeScript compiles without errors
  </done>
</task>

<task type="auto">
  <name>Task 2: Implement OCT balance fetching</name>
  <files>lib/wallet/utils.ts, lib/wallet/store.ts</files>
  <action>
    Add balance fetching functionality:

    1. In lib/wallet/utils.ts, add `fetchOctBalance(address: string): Promise<string>`:
       - Use OneChain RPC endpoint to query OCT token balance
       - RPC URL: `https://testnet-rpc.onechain.gg` (verify in research)
       - Use `move_resource` or equivalent RPC method to get CoinStore resource
       - OCT token address: `0x2::oct::OCT`
       - Return balance as string (formatted OCT amount)

    2. In lib/wallet/store.ts, add action `updateBalance(balance: string)` to update state

    3. In hooks/useWallet.ts, call fetchOctBalance after successful connection and set balance

    RPC call pattern:
    ```typescript
    const response = await fetch('https://testnet-rpc.onechain.gg', {
      method: 'POST',
      headers: { 'Content-Type': 'application/json' },
      body: JSON.stringify({
        jsonrpc: '2.0',
        method: 'move_getResource',
        params: [address, '0x1::coin::CoinStore<0x2::oct::OCT>'],
        id: Date.now(),
      }),
    });
    ```
  </action>
  <verify>
    <automated>npm run build</automated>
  </verify>
  <done>
    - fetchOctBalance function exists and calls OneChain RPC
    - Balance is fetched after wallet connection
    - Balance displays in Header component next to address
  </done>
</task>

<task type="auto">
  <name>Task 3: Create wallet unit tests</name>
  <files>__tests__/wallet/oneWallet.test.ts</files>
  <action>
    Create test file for wallet operations covering WALLET-01, WALLET-02, WALLET-03:

    Test cases:
    1. findOneWallet() returns null when no wallets registered
    2. findOneWallet() finds OneWallet when registered
    3. connectOneWallet() throws when wallet not installed
    4. connectOneWallet() calls standard:connect feature
    5. formatAddress() truncates address correctly (e.g., "0xabcd...1234")
    6. Wallet store updates state on setConnected/setDisconnected
    7. Balance updates correctly via updateBalance

    Mock the Wallet Standard API:
    ```typescript
    const mockWallet = {
      name: 'OneWallet',
      features: {
        'standard:connect': { connect: jest.fn() },
        'standard:disconnect': { disconnect: jest.fn() },
      },
      accounts: [{ address: '0xtest123' }],
    };
    ```

    Use vitest since project uses it (per 06-VALIDATION.md).
  </action>
  <verify>
    <automated>npm run test:unit -- __tests__/wallet/oneWallet.test.ts</automated>
  </verify>
  <done>
    - Test file created with 7+ test cases
    - Tests cover WALLET-01, WALLET-02, WALLET-03 requirements
    - Tests pass (green)
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 4: Wire wallet connection to UI</name>
  <files>hooks/useWallet.ts, components/layout/Header.tsx</files>
  <behavior>
    - Test 1: Clicking Connect when wallet not installed shows error
    - Test 2: Clicking Connect when installed calls connectOneWallet and displays address
    - Test 3: Balance displays after connection
    - Test 4: Clicking Disconnect clears address and balance
    - Test 5: Connecting state shows loading indicator during connection
  </behavior>
  <action>
    Update hooks/useWallet.ts to:
    1. Call fetchOctBalance in connect() after successful connection
    2. Pass balance to setConnected()

    Update components/layout/Header.tsx to:
    1. Display balance next to address (already done - verify formatting)
    2. Add loading state during connection (isConnecting)
    3. Ensure disconnect button works

    The hook should automatically fetch balance when wallet connects.
  </action>
  <verify>
    <automated>npm run build</automated>
  </verify>
  <done>
    - Connect button triggers OneWallet connection
    - Address and balance display after connection
    - Disconnect button clears state
    - Loading state visible during connection
  </done>
</task>

</tasks>

<verification>
1. OneWallet extension installed - user can connect via UI
2. Header shows truncated address (e.g., "0xabcd...1234")
3. Header shows OCT balance next to address
4. Disconnect button clears wallet state
5. All wallet tests pass: `npm run test:unit -- __tests__/wallet/`
</verification>

<success_criteria>
- WALLET-01: User can connect OneWallet extension - VERIFIED
- WALLET-02: User can view address and OCT balance - VERIFIED
- WALLET-03: User can disconnect wallet - VERIFIED
- Test coverage for wallet operations - VERIFIED
</success_criteria>

<output>
After completion, create `.planning/phases/6-onechain-integration/06-01-SUMMARY.md`
</output>
